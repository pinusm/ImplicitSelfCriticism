/* jshint asi: true */
/* global define:true */

define(['/implicit/user/pinus/sc.b1/qiatextension.sc_f.js'], function (qiatExtension) {
  return qiatExtension({
    // using a dedicated 'extension' file because I'll need to change Type 1/2 to type 3/4.
  })
  // NOTE: when you test the task, remember that pressing ESC and then ENTER skips blocks.
})
